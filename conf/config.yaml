defaults:
  - data: synth_pc_datamodule
  - model: mini_mlp
  - trainer: sit_trainer
  - _self_

# =============================================================================
# Shared experiment parameters - single source of truth
# =============================================================================
# Ambient/embedding dimension for point clouds (D in class params)
# This should be consistent across data config and model config
ambient_dim: 16
anisotropy_max_scale: 1.0
intrinsic_dim: 6

ckpt: null

hydra:
  run:
    dir: .
  output_subdir: null
