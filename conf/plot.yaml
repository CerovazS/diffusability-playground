defaults:
  - data: synth_pc
  - _self_

ambient_dim: 2

plot:
  max_points_per_class: 20000
  projection: dims        # pca | dims
  dim0: 0
  dim1: 1
  kde_levels: 20
  kde_thresh: 0.0
  cmap: rocket_r
  num_workers: 0
  out_name: media_outputs/synth_pointclouds_kde.png
  dpi: 200
